import { Sink, Source } from './pipe';
export declare const Types: {
    Array: number;
    Schema: number;
    Object: number;
};
export declare class SchemaSink extends Sink<any> {
    sink: Sink<any>;
    schemas: Map<string, number>;
    constructor(sink: Sink<any>);
    write(data: any): void;
    close(): void;
}
export declare class SchemaSource extends Source<any> {
    source: Source<any>;
    schemas: string[][];
    constructor(source: Source<any>);
    read(): any;
    iterator(options?: {
        autoClose?: boolean;
    }): Generator<any>;
    close(): void;
}
