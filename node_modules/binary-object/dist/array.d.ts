/**
 * in-memory channel to mock IO
 * */
import { Sink, Source } from './pipe';
export declare class ArraySink extends Sink<any> {
    memory: any[];
    constructor(memory: any[]);
    write(data: any): void;
    close(): void;
}
/**
 * in-memory channel to mock IO
 * */
export declare class ArraySource extends Source<any> {
    memory: any[];
    position: number;
    constructor(memory: any[]);
    read(): any;
    iterator(options?: {
        autoClose?: boolean;
    }): Generator<any>;
    close(): void;
}
