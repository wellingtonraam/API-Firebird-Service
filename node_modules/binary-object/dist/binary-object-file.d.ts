import { BinaryObjectSink, BinaryObjectSource } from './binary-object';
/**
 * support full set of javascript objects
 * including Buffer, Map, Set and Date
 *
 * but it is much slower than BinaryJsonSink, which only support JSON values
 * */
export declare namespace BinaryObjectFileSink {
    function fromFile(file: string, flags?: string): BinaryObjectSink;
}
export declare namespace BinaryObjectFileSource {
    function fromFile(file: string, flags?: string): BinaryObjectSource;
}
