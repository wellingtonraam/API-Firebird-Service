import { Sink, Source } from './pipe';
export declare class CompressJsonSink extends Sink<any> {
    sink: Sink<string>;
    constructor(sink: Sink<string>);
    write(data: any): void;
    close(): void;
}
export declare class CompressJsonSource extends Source<any> {
    source: Source<string>;
    constructor(source: Source<string>);
    read(): any;
    iterator(options?: {
        autoClose?: boolean;
    }): Generator<any>;
    close(): void;
}
