"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.genBase = void 0;
function genBase(ranges) {
    let base = 0;
    const numbers = {};
    for (const { start, count } of ranges) {
        for (let i = 0; i < count; i++) {
            const s = String.fromCharCode(start + i);
            numbers[base] = s;
            numbers[s] = base;
            base++;
        }
    }
    /** only support positive integer */
    function int_to_str(int) {
        if (int === 0) {
            return '0';
        }
        const acc = [];
        while (int !== 0) {
            const c = int % base;
            acc.push(numbers[c]);
            int = (int - c) / base;
        }
        return acc.reverse().join('');
    }
    /** only support positive integer */
    function str_to_int(str) {
        let acc = 0;
        const n = str.length;
        for (let i = 0; i < n; i++) {
            const c = str[i];
            acc = acc * base + numbers[c];
        }
        return acc;
    }
    return { int_to_str, str_to_int };
}
exports.genBase = genBase;
//# sourceMappingURL=base.js.map