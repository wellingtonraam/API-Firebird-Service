export interface CallbackSource<T> {
    read(cb: (data: T) => void): void;
    close(): void;
    iterator(): AsyncGenerator<T>;
}
export declare class StoppableCallbackSource<T> implements CallbackSource<T> {
    source: {
        read: (cb: (data: T) => void) => void;
        close?: () => void;
    };
    stopped: boolean;
    constructor(source: {
        read: (cb: (data: T) => void) => void;
        close?: () => void;
    });
    read(cb: (data: T) => void): void;
    close(): void;
    iterator(): AsyncGenerator<T>;
}
